# Line Item Properties Implementation Guide

## Обзор изменений

Эта реализация **устраняет необходимость создавать новый продукт в Shopify для каждого сгенерированного artwork**. Вместо этого используется **один фиксированный продукт** с ID `8199461339251` (вариант `44317714841715`), а кастомное изображение сохраняется в **Line Item Properties** и заменяется на клиенте через JavaScript.

## ✅ Преимущества

1. **Нет race conditions** - каждый пользователь видит свое изображение
2. **Правильные изображения везде** - в корзине, cart-drawer, на странице продукта
3. **Нет захламления каталога** - всего один продукт вместо тысяч
4. **Быстрее** - не нужно создавать продукт через Shopify Admin API

## ⚠️ Компромиссы

1. **Email уведомления** - будут содержать дефолтное изображение продукта (P_logo.png)
   - **Решение**: Можно добавить webhooks для отправки кастомных писем с правильными изображениями
2. **Заказы в Shopify Admin** - продавец увидит дефолтное изображение
   - **Решение**: Anvil ID сохраняется в properties, можно создать отдельный интерфейс для продавца

## 📋 Что изменилось

### 1. Server Code (`server_code/ServerModule1.py`)

**Было:**
- Создавали новый продукт в Shopify через `anvil_to_shopify()`
- Возвращали `variant_id` нового продукта

**Стало:**
- Загружаем изображение в Shopify CDN через `ShopifyClient.upload_image()`
- Возвращаем **фиксированный `variant_id`** + **`image_url`** + **`anvil_id`**
- НЕ создаем новый продукт

```python
# Фиксированный вариант продукта
fixed_variant_id = "44317714841715"  # ID варианта продукта 8199461339251

# Загружаем изображение в Shopify CDN
image_url = client.upload_image(row['out_image'])

return fixed_variant_id, anvil_id, image_url
```

### 2. Client Code (`client_code/Creation/__init__.py`)

**Было:**
- Получали `variant_id`, `anvil_id` от сервера
- Отправляли в Shopify

**Стало:**
- Получаем `variant_id`, `anvil_id`, **`image_url`** от сервера
- Отправляем все три параметра в `shopify_page_script.js` через postMessage

```python
# Получаем результат: variant_id, anvil_id, image_url
variant_id, anvil_id, image_url = task.get_return_value()

# Отправляем postMessage родительскому окну
send_add_to_cart(variant_id, anvil_id, image_url, add_frame)
```

### 3. Shopify Page Script (`shopify_page_script.js`)

**Новая функциональность:**

1. **Замена изображения на странице продукта** - когда пользователь генерирует artwork
2. **Сохранение в Line Item Properties** - `_image_url` и `_anvil_id`
3. **Замена изображений в корзине** - автоматически при открытии cart-drawer или /cart

```javascript
// Добавление в корзину с properties
const items = [{
  id: String(data.variant_id),
  quantity: 1,
  properties: {
    '_image_url': data.image_url,  // URL кастомного изображения
    '_anvil_id': data.anvil_id     // Anvil ID для отслеживания
  }
}];

// Замена изображений в cart-drawer и /cart
function replaceCartImages() {
  fetch('/cart.js')
    .then(response => response.json())
    .then(cart => {
      cart.items.forEach((item, index) => {
        const imageProperty = item.properties && item.properties._image_url;
        if (imageProperty) {
          // Заменяем img.src на кастомное изображение
        }
      });
    });
}
```

## 🚀 Инструкция по внедрению

### Шаг 1: Создать продукт в Shopify

1. Зайти в Shopify Admin → Products
2. Создать продукт:
   - **Title**: `Custom Art String (90x90)` (или любое название)
   - **Product Type**: String Art
   - **Vendor**: Custom String Art
   - **Price**: 390 ILS (или другая цена)
   - **Image**: Загрузить дефолтное изображение `P_logo.png`
   - **Inventory**: Untrack inventory
   - **Status**: Active
3. Скопировать **Product ID** (например, `8199461339251`)
4. Скопировать **Variant ID** (например, `44317714841715`)

### Шаг 2: Обновить код в Anvil

1. Загрузить изменения из ветки `line-item-properties`
2. Убедиться, что в `server_code/ServerModule1.py` указан правильный `fixed_variant_id`:
   ```python
   fixed_variant_id = "44317714841715"  # Ваш Variant ID
   ```

### Шаг 3: Добавить скрипт на страницу Shopify

1. Скопировать содержимое `shopify_page_script.js`
2. В Shopify Admin → Online Store → Themes → Edit Code
3. Найти файл `theme.liquid`
4. Добавить перед закрывающим тегом `</body>`:

```liquid
<script>
  {{ 'shopify_page_script.js' | asset_url | script_tag }}
</script>
```

Или вставить код напрямую:

```liquid
<script>
  (function() {
    // Вставить весь код из shopify_page_script.js
  })();
</script>
```

### Шаг 4: Добавить iframe на страницу продукта

На странице продукта `Custom Art String (90x90)` добавить iframe с Anvil приложением:

```liquid
<iframe 
  src="https://ВАШЕ_ПРИЛОЖЕНИЕ.anvil.app/_/theme/html/Create" 
  width="100%" 
  height="800px" 
  frameborder="0"
  id="string-art-iframe"
></iframe>
```

## 🧪 Тестирование

### Проверка 1: Генерация artwork
1. Загрузить изображение в iframe
2. Нажать "Create"
3. **Ожидаемый результат**: Изображение на странице продукта заменяется на сгенерированное

### Проверка 2: Добавление в корзину
1. Нажать "Add to cart" в iframe
2. **Ожидаемый результат**: 
   - Cart-drawer открывается
   - Показывается кастомное изображение (не P_logo.png)

### Проверка 3: Страница корзины
1. Перейти на `/cart`
2. **Ожидаемый результат**: Кастомное изображение отображается

### Проверка 4: Checkout
1. Перейти к checkout
2. **Ожидаемый результат**: В корзине показывается кастомное изображение

### Проверка 5: Properties
1. В консоли браузера выполнить:
   ```javascript
   fetch('/cart.js')
     .then(r => r.json())
     .then(cart => console.log(cart.items[0].properties))
   ```
2. **Ожидаемый результат**: 
   ```json
   {
     "_image_url": "https://cdn.shopify.com/...",
     "_anvil_id": "..."
   }
   ```

## 🐛 Отладка

### Проблема: Изображение не заменяется на странице продукта

**Причина**: Неверный селектор для изображения

**Решение**: 
1. Открыть DevTools на странице продукта
2. Найти элемент `<img>` основного изображения
3. Обновить селектор в `replaceProductPageImage()`:
   ```javascript
   const productMediaDiv = document.querySelector('.ВАШ_СЕЛЕКТОР');
   ```

### Проблема: Изображение не заменяется в корзине

**Причина**: Корзина загружается асинхронно

**Решение**: Увеличить задержку в `setTimeout`:
```javascript
setTimeout(replaceCartImages, 1000); // Было 500
```

### Проблема: Properties не сохраняются

**Причина**: Shopify может игнорировать properties, начинающиеся с `_`

**Решение**: Убрать подчеркивание:
```javascript
properties: {
  'image_url': data.image_url,
  'anvil_id': data.anvil_id
}
```

## 📊 Сравнение подходов

| Критерий | Старый подход (создание продуктов) | Новый подход (Line Item Properties) |
|----------|-------------------------------------|--------------------------------------|
| Race conditions | ⚠️ Возможны | ✅ Нет |
| Захламление каталога | ❌ Тысячи продуктов | ✅ Один продукт |
| Скорость | ⚠️ Медленно (API вызов) | ✅ Быстро |
| Изображение в корзине | ✅ Правильное | ✅ Правильное |
| Изображение в emails | ✅ Правильное | ⚠️ Дефолтное* |
| Изображение в Admin | ✅ Правильное | ⚠️ Дефолтное* |
| Сложность реализации | ⚠️ Средняя | ⚠️ Средняя |

*Можно решить через webhooks и кастомные email templates

## 🔮 Будущие улучшения

### 1. Webhooks для email уведомлений
Создать webhook для Order Created, который:
1. Получает line item properties
2. Заменяет изображение в email template
3. Отправляет кастомное письмо

### 2. Админка для продавца
Создать отдельную страницу в Anvil или Shopify Admin, где продавец может:
1. Просматривать все заказы с кастомными artwork
2. Видеть правильные изображения
3. Экспортировать данные для производства

### 3. Вариации размеров
Добавить поддержку разных размеров (40x40, 60x60, 90x90) как варианты одного продукта

## 📝 Коммит и деплой

```bash
cd /Users/aleksandrmalyh/dev-2025/image-app-last

# Проверить изменения
git status

# Закоммитить
git add .
git commit -m "Implement Line Item Properties approach - no more product creation"

# Запушить ветку
git push origin line-item-properties

# После тестирования - мердж в master
git checkout master
git merge line-item-properties
git push origin master
```

## 📞 Поддержка

Если возникли вопросы или проблемы:
1. Проверить консоль браузера (DevTools → Console)
2. Проверить Network tab (запросы к `/cart/add.js`)
3. Проверить `/cart.js` - убедиться, что properties сохраняются

---

**Создано**: 2026-02-18  
**Ветка**: `line-item-properties`  
**Статус**: ✅ Готово к тестированию
