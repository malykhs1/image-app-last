# 🔄 Инструкция по обновлению Shopify страницы

## ✅ Что было исправлено

1. ✅ Удален `seo.hidden` metafield - товары больше не скрыты
2. ✅ Добавлен `inventoryPolicy: CONTINUE` - товары не "sold out"
3. ✅ Улучшена обработка ошибок в JavaScript
4. ✅ Улучшена обработка postMessage в Anvil

---

## 📋 Что нужно сделать на Shopify

### Шаг 1: Обновите JavaScript на странице

Замените содержимое файла с кодом интеграции корзины (обычно это в `theme.liquid` или в секции с iframe) на новый код из файла:

📄 **`shopify_page_script.js`**

Ключевые изменения:
- Добавлена проверка статуса ответа (`response.ok`)
- Детальное логирование в консоль
- Правильная обработка ошибок 422

---

### Шаг 2: Проверьте iframe URL

Убедитесь, что в iframe передается параметр `locale`:

```liquid
{% assign current_locale = 'he' %}
{% if request.path contains '/en' %}
  {% assign current_locale = 'en' %}
{% endif %}

<iframe 
  src="https://your-app.anvil.app/?locale={{ current_locale }}"
  width="100%" 
  height="800px"
  frameborder="0">
</iframe>
```

---

### Шаг 3: Обновите ID рамки (frame)

В файле `client_code/Creation/__init__.py` (строка 13) обновите `frame_variant` на ID из вашего **нового магазина**:

**Как найти правильный ID:**
1. Shopify Admin → Products → найдите товар "Frame" (рамка)
2. Откройте вариант рамки
3. Посмотрите ID в URL:
   ```
   https://admin.shopify.com/store/YOUR-STORE/products/PRODUCT_ID/variants/VARIANT_ID
                                                                           ^^^^^^^^^^^^
   ```
4. Замените в коде:
   ```python
   frame_variant = 43092453359731  # ← Замените на новый ID
   ```

---

### Шаг 4: Тестирование

#### Тест через консоль (F12):

```javascript
fetch('/cart/add.js', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    items: [{id: YOUR_VARIANT_ID, quantity: 1}]
  })
})
.then(r => {
  console.log('Status:', r.status);
  return r.json();
})
.then(data => console.log('✅ Data:', data))
.catch(err => console.error('❌ Error:', err));
```

**Ожидаемый результат:**
```
Status: 200
✅ Data: {id: ..., quantity: 1, ...}
```

#### Тест через приложение:

1. Создайте новый artwork
2. Нажмите "Add to cart"
3. Проверьте консоль iframe:
   - Должно быть: `✅ Successfully added to cart`
4. Корзина должна открыться автоматически

---

### Шаг 5: Исправление существующих товаров (опционально)

Если у вас уже есть созданные товары, которые не добавляются в корзину:

**Вариант A: Пересоздать** (рекомендуется)
- Просто создайте новые artwork через приложение
- Они будут с правильными настройками

**Вариант B: Исправить вручную**
Для каждого проблемного товара:

1. **Shopify Admin → Products** → откройте товар
2. **Metafields:**
   - Найдите `seo.hidden`
   - Удалите или измените на `0`
3. **Inventory:**
   - Откройте вариант
   - Включите "Continue selling when out of stock"
4. Сохраните

---

## 🔍 Отладка

### Если add.js возвращает 422:

**Проверьте в консоли:**
```javascript
console.log('Sending:', JSON.stringify({items: [{id: VARIANT_ID, quantity: 1}]}));
```

**Проверьте товар в Shopify Admin:**
- Status: **Active**
- Publishing: **Online Store** ✓
- Inventory: **Continue selling when out of stock** ✓
- Metafields: **НЕТ `seo.hidden = 1`**

---

### Если товар не найден (404 на странице):

1. Подождите 30-60 секунд после создания
2. Проверьте, что товар опубликован в Online Store
3. Проверьте URL товара в Shopify Admin (должен быть handle)

---

### Если postMessage не работает:

**Проверьте в консоли iframe:**
1. F12 → Console → выпадающий список сверху
2. Выберите контекст iframe (не `top`)
3. Должны быть логи:
   ```
   CLIENT: Received postMessage (raw): {...}
   CLIENT: PostMessage action: cart_add_success
   ```

---

## ✅ Готово!

После всех шагов добавление в корзину должно работать **стабильно 100% времени**.

**Если проблемы остались:**
1. Проверьте консоль браузера (F12)
2. Проверьте консоль iframe (F12 → выберите iframe context)
3. Посмотрите Network tab → найдите запрос `/cart/add.js`
4. Проверьте Request Payload и Response

---

**Дата:** 2026-02-04  
**Приложение:** Anvil String Art Creator
