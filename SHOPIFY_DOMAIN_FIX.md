# 🚨 ОШИБКА 404: Неправильный Shopify домен

## Проблема

Ошибка: `404 Client Error for url: https://paraloom.co.il/admin/api/2025-01/graphql.json`

**Причина:** Используется кастомный домен `paraloom.co.il` вместо myshopify.com поддомена.

Shopify Admin API **НЕ работает** с кастомными доменами! Нужно использовать формат: `ВАШ-МАГАЗИН.myshopify.com`

---

## 🔍 Как найти правильный myshopify.com домен

### Способ 1: Shopify Admin

1. Зайдите в **Shopify Admin**
2. Перейдите в **Settings** → **Domains**
3. Найдите домен с форматом `*.myshopify.com`
4. Скопируйте его (например: `paraloom.myshopify.com`)

### Способ 2: URL в браузере

Когда вы заходите в Shopify Admin, посмотрите на URL:
```
https://admin.shopify.com/store/ВАШ-МАГАЗИН
                                 ^^^^^^^^^^^^
```

Ваш домен будет: `ВАШ-МАГАЗИН.myshopify.com`

### Способ 3: Проверить старый код

В старом коде использовался: `txmx0c-cc.myshopify.com`

Если это ваш текущий магазин, можно использовать этот домен.

---

## ✅ Исправление

Откройте файл `server_code/ServerModule1.py` (строка 92) и замените:

```python
# БЫЛО (НЕПРАВИЛЬНО):
shop_domain = "paraloom.co.il"

# ДОЛЖНО БЫТЬ:
shop_domain = "paraloom.myshopify.com"  # Замените на ВАШЕ значение
```

---

## 🎯 Варианты действий

### Вариант А: Использовать старый домен (если это тот же магазин)

```python
shop_domain = "txmx0c-cc.myshopify.com"
```

### Вариант Б: Найти новый домен

Если `paraloom.co.il` - это **новый магазин**, найдите его myshopify.com поддомен:

1. Зайдите в Shopify Admin
2. Скопируйте домен из Settings → Domains
3. Обновите в коде

---

## 📝 После исправления

1. Сохраните файл
2. Закоммитьте изменения:
   ```bash
   git add server_code/ServerModule1.py
   git commit -m "Fix Shopify domain - use myshopify.com instead of custom domain"
   git push origin line-item-properties
   ```
3. Протестируйте добавление в корзину

---

## ⚠️ Важно

**Shopify Admin API** работает только через `*.myshopify.com` домены!  
Кастомные домены (paraloom.co.il) работают только для **frontend** (storefront), не для API.

---

**Скажите мне правильный myshopify.com домен, и я обновлю код!**
