# 🌍 Настройка языка для Anvil iframe на Shopify

## 🎯 Проблема

Anvil приложение всегда показывается на иврите, даже на английской странице `/en`.

**Причина:** Браузер блокирует доступ к `parent.location.href` из-за cross-origin политики (Anvil на `anvil.app`, а Shopify на `paraloom.co.il`).

---

## ✅ Решение: Передать язык через параметр URL

Самый надежный способ - добавить параметр `?locale=en` или `?locale=he` в `src` iframe.

---

## 📋 Инструкция по настройке

### Шаг 1: Найдите iframe на странице Shopify

Откройте редактор темы Shopify:
- **Shopify Admin** → **Online Store** → **Themes** → **Edit code**

Найдите файл, где размещен iframe (обычно это section или page template):
- `sections/your-section.liquid`
- `templates/page.your-page.liquid`

### Шаг 2: Обновите код iframe

#### Вариант A: Статичный iframe (для тестирования)

**Для английской страницы** (`/en`):
```html
<iframe 
  src="https://your-app.anvil.app/?locale=en"
  width="100%" 
  height="800px"
  frameborder="0">
</iframe>
```

**Для ивритской страницы** (`/`):
```html
<iframe 
  src="https://your-app.anvil.app/?locale=he"
  width="100%" 
  height="800px"
  frameborder="0">
</iframe>
```

---

#### Вариант B: Динамический iframe (рекомендуется)

Автоматически определяет язык по текущему URL Shopify:

```liquid
{% comment %}
  Определяем язык по URL:
  - Если URL содержит /en → английский
  - Иначе → иврит (default)
{% endcomment %}

{% assign current_locale = 'he' %}
{% if request.path contains '/en' %}
  {% assign current_locale = 'en' %}
{% endif %}

<iframe 
  src="https://your-app.anvil.app/?locale={{ current_locale }}"
  width="100%" 
  height="800px"
  frameborder="0"
  id="anvil-artwork-app">
</iframe>

<script>
  console.log('Anvil iframe loaded with locale: {{ current_locale }}');
</script>
```

**Объяснение:**
- `{% if request.path contains '/en' %}` - проверяет, содержит ли URL `/en`
- `{{ current_locale }}` - вставляет значение переменной в src iframe
- На `/en` странице: `src="...?locale=en"`
- На `/` странице: `src="...?locale=he"`

---

#### Вариант C: С использованием Shopify локали (если настроены языки в Shopify)

```liquid
{% comment %}
  Используем встроенную локаль Shopify
{% endcomment %}

{% assign anvil_locale = 'he' %}
{% if request.locale.iso_code == 'en' %}
  {% assign anvil_locale = 'en' %}
{% endif %}

<iframe 
  src="https://your-app.anvil.app/?locale={{ anvil_locale }}"
  width="100%" 
  height="800px"
  frameborder="0">
</iframe>
```

---

### Шаг 3: Сохраните и проверьте

1. **Сохраните файл** в редакторе Shopify
2. **Откройте страницу на английском:** https://paraloom.co.il/en
3. **Откройте консоль браузера** (F12)
4. **Проверьте логи:**
   ```
   CLIENT: ✓ Locale from URL param: en
   CLIENT: ===================================
   CLIENT: FINAL LOCALE: en (from: URL parameter)
   CLIENT: ===================================
   ```
5. **Интерфейс должен быть на английском** ✓

6. **Откройте страницу на иврите:** https://paraloom.co.il/
7. **Проверьте логи:**
   ```
   CLIENT: ✓ Locale from URL param: he
   CLIENT: FINAL LOCALE: he (from: URL parameter)
   ```
8. **Интерфейс должен быть на иврите** ✓

---

## 🔍 Отладка

### Проблема: Параметр не передается

**Проверьте src iframe в браузере:**
1. Откройте страницу
2. Правой кнопкой на iframe → **Inspect Element**
3. Посмотрите атрибут `src`:
   ```html
   <iframe src="https://your-app.anvil.app/?locale=en" ...>
   ```
4. Убедитесь, что `?locale=en` присутствует

**Если параметра нет:**
- Проверьте, что Liquid код `{{ current_locale }}` работает
- Добавьте отладку: `<!-- Current locale: {{ current_locale }} -->`

---

### Проблема: В консоли пусто

**Откройте консоль iframe:**
1. Правой кнопкой на iframe → **Inspect Element**
2. В DevTools: **Console** → выпадающий список сверху
3. Выберите iframe контекст (не `top`)
4. Теперь вы видите логи Anvil приложения

---

### Проблема: Язык все еще иврит

**Проверьте в консоли iframe:**
```
CLIENT: ✓ Locale from URL param: en  ← Должно быть 'en' для английской страницы
```

Если видите `he` вместо `en`:
- Проверьте Liquid логику определения языка
- Убедитесь, что на английской странице URL содержит `/en`

---

## 📊 Приоритет определения языка

Anvil приложение использует следующий приоритет:

1. **Параметр URL** `?locale=en` (самый надежный) ⭐
2. **Parent URL** `parent.location.href` (может быть заблокирован)
3. **Document referrer** `document.referrer` (может быть пустым)
4. **Default:** иврит

**Рекомендуется использовать метод #1** (параметр URL) для надежности.

---

## 🎉 Результат

После настройки:
- ✅ https://paraloom.co.il/en → английский интерфейс
- ✅ https://paraloom.co.il/ → ивритский интерфейс
- ✅ Работает надежно (не зависит от cross-origin)
- ✅ Автоматическое определение языка

---

## 📝 Пример полного кода для Shopify

```liquid
{%- comment -%}
  String Art Anvil App with automatic locale detection
{%- endcomment -%}

{% assign anvil_locale = 'he' %}
{% if request.path contains '/en' %}
  {% assign anvil_locale = 'en' %}
{% endif %}

<div class="artwork-section">
  <iframe 
    id="anvil-artwork-app"
    src="https://your-app.anvil.app/?locale={{ anvil_locale }}"
    width="100%" 
    height="800px"
    frameborder="0"
    allow="camera; microphone"
    loading="lazy">
  </iframe>
</div>

{% comment %} Отладочная информация (можно удалить после проверки) {% endcomment %}
<script>
  console.log('Anvil app loaded with locale: {{ anvil_locale }}');
  console.log('Current path: {{ request.path }}');
</script>
```

---

## ✅ Готово!

После добавления параметра `?locale=en` в src iframe, приложение будет автоматически определять язык и показывать правильный интерфейс!

